参考资料：https://morvanzhou.github.io/tutorials/data-manipulation/np-pd/2-5-np-indexing/

Numpy索引
1. 一维索引
当矩阵为一维时，A[0]为一个元素，当矩阵为二维时，A[0]是一个一维矩阵

2. 二维索引
2.1 当矩阵为二维时，得到单个元素的方法有两种，A[0][0]或者A[0,0]
2.2 :可以用于一定范围内的切片操作，A[0,0:3]

3 生成器
生成器的引入原因：通过列表生成式创建的列表占用很大的存储空间，如果我们仅仅需要访问前面几个元素，那后面绝大多数元素占用的空间都浪费了。
生成器机制：一边循环一边计算，generator保存的是算法，每次通过for循环迭代调用时，就计算出g的下一个元素的值，直到计算到最后一个元素，没有更多元素时，则抛出错误；
定义生成器的多种方法：
	g = (x*x for x in range(10))
	yield
生成器与函数的执行流程:
	函数是顺序执行，遇到return语句或者最后一行函数语句就返回。
	生成器每次遇到yield语句返回，再次执行时返回的yield语句处继续执行
总结：
	生成器的工作原理是在for循环的过程中不断计算出下一个元素，并在适当条件结束循环

4 迭代器
可迭代对象（Iterable）：可以直接作用于for循环的对象统称为可迭代对象
迭代器：可以被next()函数调用并不断返回下一个值的对象成为迭代器
生成器都是迭代器对象，list,dict,str虽然是可迭代对象，但是不是迭代器
将可迭代对象转化为迭代器的方法，iter()
生成器的计算是惰性的，只有在需要返回下一个数据时才会计算。


